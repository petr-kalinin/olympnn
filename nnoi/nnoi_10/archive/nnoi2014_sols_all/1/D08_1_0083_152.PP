const eps=0.000000001;
var a,b,s:double;
begin
read(a,b,s);
if abs(s-a-b)<eps then write(1)
                  else write(0);
end.
{мне намного удобнее работать в Pascal ABC, так как там красивее интерфейс,
лучше отладка и можно развернуть его на весь экран. Но там нет никаких вещественных
типов кроме real. Если надо (как сейчас), могу писать и на FP. Что не так с задачей
IP-адрес?}